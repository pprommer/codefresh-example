# Build service with unit tests
version: '1.0'

steps:
  buildimage:
    type: build
    image_name: pprommer/codefreshexample 
    working_directory: ./
    dockerfile: Dockerfile

  unit_test:
    image-name: pprommer/codefreshexample 
    working-directory: ./ 
    commands:
      - npm run test 
