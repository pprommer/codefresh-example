# Build service with unit tests
version: '1.0'

steps:
  buildimage:
    type: build
    image_name: pprommer/codefreshexample 
    working_directory: ./
    dockerfile: Dockerfile
    tag: latest

  unit_test:
    image: pprommer/codefreshexample 
    working-directory: ./ 
    commands:
      - npm run test 
